import React from "react";

import Ref from "./examples/Ref";

function App() {
  /** Simular unmount de component */
  /** Importar 'useState' da lib 'react' para o teste abaixo */

  // const [hidden, setHidden] = useState(false);

  // setTimeout(() => {
  //   setHidden(true);
  // }, 3000);

  // return <> {!hidden && <Effect />}</>;

  return <Ref />;
}

export default App;
